export default class EventManager<ListenerType> {
    protected readonly _listeners: Set<ListenerType>;
    get listeners(): ListenerType[];
    addListener(listener: ListenerType): void;
    removeListener(listener: ListenerType): void;
}
