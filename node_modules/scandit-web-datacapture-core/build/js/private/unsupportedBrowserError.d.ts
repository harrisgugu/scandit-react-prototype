import { DataCaptureError } from "../DataCaptureError";
import type { BrowserCompatibility } from "../browserCompatibility";
export declare class UnsupportedBrowserError extends DataCaptureError {
    readonly data?: BrowserCompatibility;
    constructor(browserCompatibility: BrowserCompatibility);
}
