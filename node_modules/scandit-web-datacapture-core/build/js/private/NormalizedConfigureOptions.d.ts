import type { LoadingStatusSubscriber } from "../LoadingStatus";
import type { ConfigureOptions } from "../index";
import type { Logger } from "../logger";
import type { OverrideState } from "../worker";
/**
 * @hidden
 * Hidden
 * */
export interface NormalizedConfigureOptions extends ConfigureOptions {
    loadProgressNotifier?: LoadingStatusSubscriber;
    logLevel: Logger.Level;
    overrideThreadsSupport: OverrideState;
    overrideSimdSupport: OverrideState;
}
