import type { SizeJSON } from "scandit-web-datacapture-core";
import { Size } from "scandit-web-datacapture-core";
import type { Serializable } from "scandit-web-datacapture-core/build/js/private/Serializable";
export interface PrivateTrackedBarcodeView {
    data: string;
    getEncodedImageData: (element: HTMLElement) => string;
}
interface TrackedBarcodeViewOptionsJSON {
    size?: SizeJSON;
    scale?: number;
}
export interface TrackedBarcodeViewOptions {
    size?: Size;
    scale?: number;
}
export interface TrackedBarcodeViewJSON {
    data: string;
    options: TrackedBarcodeViewOptionsJSON | null;
}
export declare class TrackedBarcodeView implements Serializable<TrackedBarcodeViewJSON> {
    private readonly data;
    private readonly options;
    private constructor();
    static withHTMLElement(element: HTMLElement, options: TrackedBarcodeViewOptions | null): Promise<TrackedBarcodeView>;
    static withBase64EncodedData(data: string, options: TrackedBarcodeViewOptions | null): Promise<TrackedBarcodeView>;
    private static getEncodedImageData;
    private static getSize;
    private static getSVGDataForElement;
    private static getCanvasWithSize;
    private static getBase64DataForSVG;
    toJSONObject(): TrackedBarcodeViewJSON;
}
export {};
