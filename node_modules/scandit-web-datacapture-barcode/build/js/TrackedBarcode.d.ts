import type { QuadrilateralJSON } from "scandit-web-datacapture-core";
import { Quadrilateral } from "scandit-web-datacapture-core";
import type { BarcodeJSON } from "./Barcode";
import { Barcode } from "./Barcode";
export interface TrackedBarcodeJSON {
    barcode: BarcodeJSON;
    deltaTime: number;
    identifier: string;
    location: QuadrilateralJSON;
    anchorPositions: QuadrilateralJSON;
    shouldAnimateFromPreviousToNextState: boolean;
}
export declare class TrackedBarcode {
    private _barcode;
    private _deltaTime;
    private _identifier;
    private _location;
    private _anchorPositions;
    private _shouldAnimateFromPreviousToNextState;
    private sessionFrameSequenceID?;
    get barcode(): Barcode;
    get deltaTime(): number;
    get identifier(): number;
    get location(): Quadrilateral;
    get anchorPositions(): Quadrilateral;
    get shouldAnimateFromPreviousToNextState(): boolean;
    private static fromJSON;
}
